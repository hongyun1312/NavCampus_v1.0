# 生产环境配置
spring:
  datasource:
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20

logging:
  level:
    com.hongyun.navcampus: INFO
    org.springframework.security: WARN

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
